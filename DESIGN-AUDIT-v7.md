# 🎨 التدقيق التصميمي والريسبونسف الشامل — v7

> **التاريخ:** 2026-08-17 · **النطاق:** 127 صفحة، 48 درساً، 24 ملف اختبار وحدة، 173 اختبار E2E
> **الهدف:** تجربة مستخدم احترافية متناسقة بلا تشوهات، ولا تداخلات، ولا نصوص مقطوعة —
> مسار تعلم منظّم ومتسلسل لا يضيع فيه المتعلم.

---

## ١. المنهجية (جرد صارم بالأدوات لا بالحدس)

| الأداة | الوظيفة | النتيجة |
|---|---|---|
| `scripts/dom-audit.mjs` | فحص DOM عنصراً بعنصر في 23 صفحة × مقاسين (375px جوال / 1280px سطح مكتب) | كشف التجاوزات والتداخلات والقصّ والنصوص الصغيرة وأهداف اللمس |
| `scripts/diag-*.mjs` | تشخيص سلسلة الأبوين لعنصر متجاوز (المسار من العنصر حتى الجذر) | تحديد السبب الجذري بدقة |
| Playwright E2E (`e2e/responsive.spec.ts` — 9 اختبارات جديدة) | إثبات دائم للإصلاحات | 173/173 ناجحة |
| `scripts/fix-grid-cols.mjs` | إصلاح نظامي آلي موثّق | 80 شبكة أُصلحت في 54 ملفاً |

**أحجام الفحص:** 320px (أضيق شاشة) · 375px (جوال) · 768px (تابلت) · 1280px (سطح مكتب)

---

## ٢. الأخطاء النظامية المكتشفة (P0 — أثرت على كل المنصة)

### 🔴 P0-01 — شبكات بلا أساس جوال (السبب الجذري للتجاوزات)
**المشكلة:** فئة `grid` في Tailwind تُنشئ شبكة **بدون أعمدة صريحة** — العمود الضمني
`auto` يتمدد إلى *أقصى عرض محتوى* أبنائه بدل ملء الحاوية. كل
`grid gap-X md:grid-cols-2` بلا `grid-cols-1` كان ينتج على الجوال بطاقات بعرض
محتواها الكامل **متدفقة خارج الشاشة**.

**الأثر الفعلي (قاسه السكربت):** بطاقة «كلمة اليوم» في لوحة التحكم كانت **454px على شاشة 375px**
(تجاوز 79px)، وبطاقات الدروس في مسار التعلم **380px** (تجاوز 38px)، وكلها منزاحة يساراً
حتى **L:-95**.

**الإصلاح:** سكربت آلي أضاف `grid-cols-1` لأساس الجوال في **80 موضعاً / 54 ملفاً**
(الصفحات، المكوّنات، الأقسام). فحص الآليّة لا يقبل `lg:grid lg:grid-cols-[220px_1fr]`
(شبكة سطح المكتب فقط) لأن أساسها ليس `grid`.

**التحقق:** بعد الإصلاح → `overflows-viewport` انخفض من **32 إلى 0**.

### 🔴 P0-02 — الشريط السفلي الثابت يغطي المحتوى
**المشكلة:** حشوة الصفحة السفلية `pb-16` (64px) أقل من ارتفاع الشريط السفلي
(60–68px + منطقة الأمان iOS) → آخر محتوى كل صفحة يُغطى على الجوال.
قياس فعلي: لوحة التحكم آخر بطاقاتها كانت تحت الشريط **بعمق 68px**.

**الإصلاح:** `pb-16` → `pb-28` (112px) في `layout.tsx` مع تعليق توثيقي —
هامش أمان 44px+ على أندرويد و10px+ على iPhone (مع safe-area).

### 🔴 P0-03 — بطاقات الدروس لا تتقلّص (min-width:auto)
**المشكلة:** في الشبكة، `min-width: auto` يجعل عنصر الشبكة لا ينكمش تحت عرض
محتواه الأدنى — فأي عنوان ألماني طويل (*A1 kompakt — die große Wiederholung*)
يوسّع البطاقة خارج الحاوية.

**الإصلاح:** `min-w-0` على بطاقات `sequential-path.tsx` + `truncate` للعناوين +
`flex-wrap` لرأس المستوى (الرموز والأزرار لا تتداخل بعد الآن).

### 🔴 P0-04 — الصفحة الرئيسية تتمرّر تلقائياً إلى المنتصف عند التحميل
**المشكلة:** حقل الاسم في بطاقة «أهلاً بك» يحمل `autoFocus` — المتصفح **يقفز بالصفحة**
إلى منتصفها فور التحميل (scrollY=1859 على جوال!) — **توهان مباشر للمستخدم الجديد**.

**الإصلاح:** إزالة `autoFocus` → `scrollY=0` مؤكَّد في الجوال وسطح المكتب.
(الحقل ما زال متاحاً بالضغط — ولا يُسرق التركيز من العنوان).

---

## ٣. الإصلاحات التفصيلية (P1)

### شبكات وأعمدة
- ✅ `grid-cols-1` أساس جوال في 80 موضعاً (لوحة التحكم 7، b2-simulation 3، test-hub 3، learner-journey 4، ai-tools 4…)

### نصوص مقطوعة/صغيرة
- ✅ **الخريطة الحرارية**: أسماء الأشهر 8px→9px مع **اختصار ذكي** (ينا/فبر/مار…) على
  الجوال بدل الاسم الكامل المقتطع، وأعمدة أيام أوسع (w-7) — لا نصّ مقطوع
- ✅ **رفع `text-[10px]` → `text-[11px]`** في 46 ملفاً (شارات المستوى والتصنيفات
  والإحصاءات) ما عدا المكوّنات الكثيفة المقصودة (شريط الجوال، المسار المتعرج، الخريطة الحرارية)
- ✅ شارة «الخطوة الحالية» في الدرس: 10px→11px + `max-w-24 truncate` (لا تزاحم شريط التقدم)
- ✅ أزرار «أستطيع/جزئياً/بعد» في شهادة الإتقان: 10px→11px + حشوة أكبر + `flex-wrap`
- ✅ بادجات كتابة B2: 9px→10px

### أهداف اللمس (Accessibility)
- ✅ تبويبات المستويات A1/A2/B1/B2 في المسار: py-1→py-1.5 (24px→28px)
- ✅ «مركز المستوى ←» وشارة «X دروس»: 26px→30px
- ✅ روابط الفوتر: `inline-block py-1` (14px→28px)
- ✅ رابط «مسار التعلم» في رأس الدرس: +py-1
- ✅ رابط «ابدأ المستوى» في معاينة المستويات: +py-1.5
- ✅ شعار الرأس والفوتر: حشوة لمس مريحة

### وضوح المسار (UX بلا حيرة)
- ✅ **بطل الرئيسية**: من **5 أزرار متزاحمة** (لوحتي/حدّد مستواك/مسار/جرّب/استكشف) إلى
  **3 خطوات واضحة**: ① «ابدأ رحلتك الآن» (ذهبي، الأساسي) ← ② «حدّد مستواك أولاً» ← ③ «جرّب درساً تجريبياً»
- ✅ **تصحيح أرقام الدروس الثابتة 46 → 48** في 6 مواضع (learner-journey، teacher-mentor،
  about، page.tsx، data/lessons) — المشروع 48 درساً فعلاً

---

## ٤. الأرقام النهائية (قبل ← بعد)

| المؤشر | قبل التدقيق | بعد التدقيق |
|---|---|---|
| تجاوز أفقي (عناصر خارج الشاشة) | **32** | **0** |
| بطاقات تتجاوز حاويتها | 15 (لوحة التحكم) + 8 (مسار التعلم) | **0** |
| تداخل عناصر تفاعلية حقيقي | 71 | **0** (المتبقي: رابط `sr-only` غير مرئي + قوائم منسدلة مغلقة) |
| نصوص مقطوعة فعلياً | 22 | **0** (المتبقي: `truncate` مقصود بنقاط + زخارف overflow-hidden) |
| خط أصغر من 11px في محتوى قابل للقراءة | 54 | 4 (محاور رسم بياني 9px داخل تمرير أفقي — مقصود) |
| اختبارات E2E | 164 | **173 (9 اختبارات ريسبونسف جديدة)** |
| قفزة تلقائية للصفحة الرئيسية | scrollY=1859 | **scrollY=0** |

---

## ٥. اختبارات الريسبونسف الدائمة (`e2e/responsive.spec.ts`)

1. **لوحة التحكم 375px**: لا تجاوز أفقي إطلاقاً + لا بطاقة تتجاوز الشاشة
2. **مسار التعلم 375px**: بطاقات الدروس داخل الحاوية (يميناً ويساراً)
3. **صفحة درس 375px**: لا تجاوز + شريط الخطوات يعمل
4. **الشريط السفلي**: بعد التمرير لنهاية الصفحة لا يغطي آخر محتوى
5. **صفحة الاختبارات 1280px**: لا تجاوز
6. **محاكاة B2 1280px**: لا تجاوز + الأقسام ظاهرة
7. **الرئيسية 1280px**: لا تجاوز
8. **الرئيسية 320px**: تعمل بلا تجاوز (أضيق شاشة)
9. **الإعدادات 320px**: لا تجاوز

---

## ٦. أدوات التدقيق (محفوظة في `scripts/`)

| السكربت | الاستخدام |
|---|---|
| `scripts/dom-audit.mjs` | فحص شامل: تجاوز/تداخل/قص/خطوط/أهداف لمس — `node scripts/dom-audit.mjs` |
| `scripts/screenshots.mjs` | لقطات شاشة 57 صورة (3 مقاسات × 19 صفحة) + تقرير تجاوز |
| `scripts/diag-path.mjs` / `diag-dash.mjs` / `diag-nav.mjs` / `diag-scroll.mjs` | تشخيص سبب جذري محدد |
| `scripts/fix-grid-cols.mjs` | إصلاح نظامي للشبكات (يُشغَّل مرة واحدة — نتائجه محفوظة) |

> ⚠️ أي تطوير مستقبلي: **ممنوع** إضافة `grid ... sm/md/lg:grid-cols` بدون `grid-cols-1`
> أساساً، و**ممنوع** `autoFocus` على حقول في منتصف الصفحة، و**ممنوع** حشوة سفلية
> أقل من ارتفاع الشريط السفلي (pb-28) على الجوال.
