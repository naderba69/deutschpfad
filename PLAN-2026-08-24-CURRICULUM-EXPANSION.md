# خطة توسعة المنهج — مبنية على قياس، لا على انطباع

> **التاريخ:** 2026-08-24 · **الأساس:** `a918690` · **الحالة:** مقترح للإقرار قبل التنفيذ

---

## 1. تصحيح جوهري لتشخيص الجلسة السابقة

في إعادة الاحتساب قلتُ إن السقف هو «عدد الدروس: 49 مقابل 248 موعوداً». **هذا التشخيص كان ناقصاً، وأصحّحه هنا.**

قِستُ سطح المحتوى غير الدرسي الذي لم يدخل مراحل التدقيق السبع إطلاقاً:

| المورد | العدد | التوزيع على المستويات |
|---|---|---|
| قصص تفاعلية `INTERACTIVE_STORIES` | **60** | 15 / 15 / 15 / 15 |
| حوارات يومية `DAILY_DIALOGUES` | **60** | 15 / 15 / 15 / 15 |
| بودكاست `PODCASTS` | **20** | 4 / 5 / 5 / 6 |
| بطاقات ثقافية `CULTURE_CARDS` | 16 | — |
| مهام كتابة `WRITING_TASKS` | 28 | 2 / 2 / 2 / 22 |
| مجموعات امتحان B2 | Lesen ×3 + Hören ×3 | — |

**نتيجتان تغيّران الصورة:**

1. **الوعد المعروض في `about/page.tsx` («60 قصة + 20 بودكاست») صادق حرفياً** — تحقّقت منه بالعدّ. لم يكن ادّعاءً.
2. **التعرّض المعجمي الحقيقي = 2,424 كلمة فريدة** (لا 419 كما توحي البطاقات وحدها): A1 795 · A2 869 · B1 949 · B2 1072. الوعد المعروض 3,600 كلمة، فالفجوة **نسبتها 33% لا 88%**.

⇒ المنهج **ليس هيكلاً فارغاً**. المشكلة ليست «كمّ المحتوى» بل **أن المحتوى الرديف غير مُدمَج في مسار التعلّم**: الطالب في `a1-03` لا يُوجَّه إلى قصة A1 المناسبة ولا إلى الحوار المرتبط.

---

## 2. الفجوات النحوية الحقيقية (الأهم للوصول إلى B2)

فحصتُ 108 كتلة نظرية عبر المستويات الأربعة، وميّزتُ بين **«مذكورة عرَضاً»** و**«مُدرَّسة بكتلة نظرية»**:

| القاعدة | الحالة | الخطورة | الموضع الصحيح |
|---|---|---|---|
| `damit` / `um … zu` (الغرض) | ❌ **لا كتلة نظرية** — ترد في حوارات فقط | 🔴 حرجة | B1 |
| `wegen/trotz/während` + Genitiv | ❌ **لا تُدرَّس** (التطابق في a2-12 كان عرضياً داخل عنوان عن الروابط) | 🔴 حرجة | B1 |
| `dessen` / `deren` (وصل بالمضاف) | ❌ ترد كخيار مُشتِّت في تمارين فقط | 🔴 حرجة | B2 |
| Präpositionaladverbien (`darauf`, `worauf`) | ❌ **صفر** كتلة — لا غنى عنها مع أفعال حروف الجر | 🔴 حرجة | B2 |
| `je … desto` | ⚠️ صفّ جدول واحد في `b1-08` بلا كتلة ولا تمرين مخصّص | 🟠 متوسطة | B1 |
| `sodass` (النتيجة) | ⚠️ سرد ضمن `b2-05/06` بلا كتلة | 🟠 متوسطة | B2 |
| الأسئلة غير المباشرة | ❌ لا كتلة | 🟠 متوسطة | B1 |
| المصدر المزدوج (`hat kommen können`) | ❌ لا كتلة | 🟠 متوسطة | B2 |

**الأربع الحمراء تمنع فعلياً بلوغ B2:** `darauf/worauf` مطلوبة لكل فعل بحرف جر، و`dessen/deren` و`wegen+Genitiv` تردان في كل نص B2 أصيل.

---

## 3. الخطة — ثلاث مراحل مرتّبة بالأثر ÷ التكلفة

### المرحلة أ — سدّ الفجوات النحوية الحرجة (الأعلى أثراً)
أربعة دروس جديدة، كلٌّ بالبنية الكاملة (10 أقسام إلزامية، ≥3 كتل نظرية، ≥20 بند تدريب، ≥10 بطاقات):

| الدرس المقترح | الوحدة المضيفة | المحتوى |
|---|---|---|
| `b1-12` الغرض والسبب | B1 | `damit` مقابل `um … zu` (شرط وحدة الفاعل) + `wegen/trotz/während` + Genitiv |
| `b1-13` الأسئلة غير المباشرة والتناسب | B1 | `ob`/أدوات الاستفهام في الجملة التابعة + `je … desto` |
| `b2-11` الوصل المتقدّم | B2 | `dessen`/`deren` + وصل بحرف جر + `was`/`wo` |
| `b2-12` ظروف حروف الجر | B2 | `darauf/worauf/damit/womit` + `sodass` + المصدر المزدوج |

**الأثر على الدرجة:** كل درس جديد يرفع وحدته إلى وفاء أعلى وعمق 10/10 ⇒ تقديري **+3 إلى +5 نقاط** على المعدّل العام، فضلاً عن سدّ ثغرة تعليمية حقيقية.

### المرحلة ب — ربط المحتوى الرديف بالدروس (الأرخص كلفةً)
إضافة حقل `relatedResources` إلى `LessonMeta` يربط كل درس بقصته وحواره وبودكاسته المناسبة (60+60+20 = 140 مورداً معطَّلاً حالياً).
**لا يتطلّب كتابة محتوى جديد إطلاقاً** — المحتوى موجود ومكتوب، ينقصه التوجيه فقط. هذا أعلى عائد لكل ساعة عمل في الخطة كلها.

### المرحلة ج — رفع الكثافة والعمق في الوحدات الضعيفة
33 وحدة عندها كتلتان نظريتان بدل ثلاث (متوسط العمق 7.26/10)، ومتوسط التدريب 15.8 بنداً مقابل سقف 20.
عمل تدريجي: كتلة ثالثة + 4 بنود تدريب + بطاقتان لكل وحدة دون 70.

---

## 4. ما لا أوصي به

- **❌ كتابة 199 درساً لبلوغ الـ248 الموعودة.** الرقم 248 كان حقلاً يدوياً متعفّناً حُذف من المصدر، لا التزاماً تربوياً. منهج A1–B2 جادّ يُبنى عادةً على 60–90 درساً مكثّفاً. **الأصحّ تعديل الوعد لا مطاردته.**
- **❌ حشو البطاقات لرفع معيار المفردات.** التعرّض الحقيقي 2,424 كلمة؛ المشكلة في تنظيم المراجعة لا في العدد.
- **❌ C1/C2** — خارج النطاق بقرارك.

---

## 5. سؤال قرار قبل التنفيذ

المرحلة **أ** تسدّ ثغرات تعليمية حقيقية تمنع بلوغ B2. المرحلة **ب** أرخص وتُفعّل 140 مورداً جاهزاً. أيّهما أبدأ؟
