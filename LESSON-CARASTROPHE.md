# 🚨 درس الكارثة (v5.4→v5.5) — ما حدث وكيف أُصلح

> **التاريخ:** 2026-08-15 · **الخلاصة:** تعديل CSS واحد كسر كل الأزرار على الجوال.
> **المبدأ الجديد:** لا قواعد CSS عمومية `!important` على عناصر تفاعلية — أبداً.

---

## ما حدث (كارثة v5.4)

في جولة «تحسين UI/UX» أضفت قاعدة عمومية:

```css
@media (max-width: 480px) {
  .whitespace-nowrap {
    white-space: normal !important;  /* ❌ كارثة */
  }
}
```

فئة `whitespace-nowrap` هي **أساس كل أزرار Button** في المشروع (`src/components/ui/button.tsx`).
النتيجة على الجوال:
- كل زر يلتفّ نصه → يتمدد عمودياً → **يحتدم بصناديق الجمل** (الشكوى: «البوكسات الجمل فيها حروف مقطعة وبالطول ومتداخلة مع الاستماع والنطق»).
- أزرار الاستماع/النطق الصغيرة أصبحت صفوفاً ضخمة.

## الإصلاحات الأربعة (v5.5)

### 1) حذف القاعدة الكارثية نهائياً + تعليق تحذيري في globals.css
### 2) زر «أتممت هذا القسم» و«التالي» — معالجة موضعية فقط:
   - `flex-wrap` على الحاوية + `whitespace-normal` **على الزر نفسه** — يلتفّ عند الحاجة فقط دون كسر بقية الأزرار.
### 3) مشكلة «أول جملة تتفاعل والباقي لا» — جذرها حقيقي:
   - **Web Speech API يسمح بجلسة واحدة عبر الصفحة.** كان «انطقها» في السطر 2 يرفضه المتصفح بصمت لأن جلسة السطر 1 لم تُدمَّر.
   - **الحل:** مدير مركزي `stopActiveRecognition()` في `speech-recognition.ts` — يُوقف أي جلسة نشطة قبل بدء جديدة.
### 4) «أول جملة تُنطق فقط» في الحوار/البودكاست:
   - `playDialogue` و `speakSequence` كانتا تضيفان كل الأسطر دفعة واحدة فور `cancel()` — Chrome يبتلع بعضها.
   - **الحل:** تسلسل عبر `onend` — كل جملة بعد انتهاء سابقتها حتماً.
   - البودكاست: تقطيع النص جملة جملة قبل النطق.

## اختبارات الحماية الجديدة

| الملف | يثبت |
|-------|------|
| `e2e/listen-fix.spec.ts` (4) | أزرار بحجم طبيعي · لا زر بديل يزحم · «انطقها» يعمل في أكثر من سطر · لا فيضان 320px |
| `e2e/speech.spec.ts` (5) | التقييم التلقائي · كشف الوهمي · البديل · استماع بلا أخطاء |
| `e2e/textarea.spec.ts` (3) | صناديق تتمدد · لا قص أفقي |
| `e2e/uiux.spec.ts` (3) | زر طويل يلتفّ داخل البطاقة · كلمات طويلة · جداول قابلة للتمرير |

## النتائج النهائية

```
✅ 143/143 E2E · 149/149 وحدة · TSC 0 · ESLint 0 · بناء 121 صفحة
✅ ZIP: 538 ملفاً — نظيف وسليم
```

## المبادئ الممنوعة من الآن (مكتوبة كقاعدة دائمة)

1. **❌ لا `!important` عمومي على عناصر تفاعلية** — عالج الحالة الموضعية فقط.
2. **❌ لا كسر `whitespace-nowrap` الأساسي** — استخدم `whitespace-normal` على الزر نفسه.
3. **❌ لا إضافة عدة `speechSynthesis.speak()` دفعة واحدة** — دائماً تسلسل `onend`.
4. **✅ أي إصلاح CSS/تفاعلي يُثبت باختبار E2E على 320px و390px** قبل البناء.
