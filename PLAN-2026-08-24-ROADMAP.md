# خارطة الطريق — من الصفر إلى شهادة B2 على هذا الموقع وحده

التاريخ: 2026-08-24 · الفرع: `arena/01a0299b-deutschpfad`

> **الهدف الوحيد:** أن يكون هذا الموقع أداةَ الدراسة الوحيدة — كمّاً وكيفاً — حتى اجتياز امتحان اللغة الألمانية.
> **هذه وثيقة قرارات مُتَّخذة، لا قائمة خيارات.** كلّ ما فيها محسوم؛ ما يبقى هو التنفيذ.

---

## ١. المرجع: واحدٌ لا اثنان

**المعتمد: CEFR (للتدرّج) + Goethe-Zertifikat (للامتحان).** وهو المرجع القائم فعلاً في 115 ملفاً من المشروع.

**قرار:** لا يُضاف أي كتاب مقرّر (Schritte · Menschen · Netzwerk) مرجعاً بنيوياً. استُعمل Schritte مرّةً واحدةً **مسطرةً للقياس** في `STATUS-2026-08-24-A1-VS-SCHRITTE.md`، وقد أدّى غرضه. لا مراجع متداخلة.

**السبب:** المرجعان من جنسين مختلفين. Goethe يحدّد **ماذا** يجب أن يُتقَن في كل مستوى؛ الكتاب المقرّر يحدّد **في أي درس** يُدرَّس — وهو قرار تنفيذيّ من قرارات ممكنة، لا قانون. نملك قرارنا التنفيذي الخاص، وهو مشروع ما دام يخدم مرجع «ماذا».

**تصحيح مطلوب:** يزعم `COVERAGE.md:42` أنّ المنهج «مطابق لمنهج Goethe/Telc/Menschen». هذا الادّعاء غير مسنود ويُصحَّح عند إنجاز A1.

---

## ٢. الفجوات الحرجة — وأين تُسدّ (محسوم)

ثلاث بنيات يفترض **امتحان Goethe A1 نفسه** إتقانها، ونؤجّلها اليوم إلى A2:

| # | الفجوة | القرار | المبرّر |
|---|---|---|---|
| ١ | **Perfekt** `habe gemacht / bin gefahren` | **`a1-14` يُعاد بناؤه درسَ الماضي** | `a1-14` اليوم يعلّم الأرقام 0–100 — مادّة `a1-07` نفسها (0–1000). أنحف درس في المنهج (211 حرفاً، 7 تمارين). استبداله يحلّ عيبين بحركة واحدة دون توسيع المسار. |
| ٢ | **الدَّاتيف التواصليّ** `Das gefällt mir` | **كتلة `t4` في `a1-08`** | سياق الملابس هو موطنه الطبيعي: «يعجبني هذا القميص / لا يناسبني المقاس». `gefallen · passen · stehen` كلّها في المحلّ نفسه. |
| ٣ | **ضمائر النصب** `Ich sehe ihn` | **كتلة جديدة في `a1-11`** | درس المدينة يقتضيها: «أعرفه · أراها · نأخذه». |

وفجوات أصغر تُدرَج ضمناً أثناء إعادة البناء: `wollen/sollen` (في `a1-14` مع الماضي) · **`doch`** جواباً للسؤال المنفيّ (في `a1-08` — صفر ورود اليوم في A1 كلّه) · `unser/euer` (في `a1-10`) · الشقّ المنصوب من Wechselpräpositionen (في `a1-11`).

**أثرٌ ارتداديّ محسوم:** درس `a2-01 Reisen und Urlaub` كتلتاه النظريتان هما Perfekt وPerfekt mit haben/sein حصراً. بعد صعود Perfekt إلى A1 يُعاد بناء `a2-01` على **Perfekt المتقدّم**: الأفعال المنفصلة وغير المنفصلة، `-ieren`، ترتيب الجملة الطويلة. لا يُحذف الدرس ولا يُفرَّغ.

---

## ٣. معيار الدرس المكتمل

هذه العتبات مفروضة آليّاً عبر `src/data/lessons/academic-depth.test.ts` (قائمة `ACADEMIC_LESSONS`). درسٌ لا يجتازها لا يُعَدّ منجزاً.

| العنصر | العتبة |
|---|---|
| الشرح لكل كتلة | ≥900 حرف، فقرات لا سطر واحد (الفعليّ المحقَّق: 1700–2300) |
| `whyAr` · `comparisonWithArabic` | ≥250 حرفاً لكلٍّ |
| الأمثلة · الأخطاء الشائعة | ≥6 أمثلة · ≥3 أخطاء بتعليل ≥60 حرفاً |
| نصّ القراءة | إلزاميّ — A1 ≥90 كلمة (المحقَّق 150–250)، A2 ≈250، B1 ≈320، B2 ≈400 |
| المفردات المشروحة | ≥8، كلّها واردة في النصّ فعلاً |
| أسئلة الفهم | ≥4 بخيارات وتعليل |
| بنك التدريب | ≥14 بنداً على ≥5 أنماط مختلفة |
| البطاقات | ≥12 |

---

## ٤. حالة المنهج — قياسٌ فعليّ

**منجَز (12):** `a1-00` · `a1-01` · `a1-02` · `a1-03` · `a1-04` · `a1-05` · `a1-06` · `a1-07` · `a1-08` · `a1-09` · `a1-10` · `a1-11`
— متوسّط الشرح ~1500 حرف · نصّ قراءة في كلٍّ · 15–26 تمريناً · 13–20 بطاقة · جميعها محروسة بـ`ACADEMIC_LESSONS`.

**الفجوات الأربع المطويّة (منجَزة):**

| الفجوة | الموضع | الحالة |
|---|---|---|
| أفعال الدّاتيف gefallen/passen/stehen | `a1-08:t4` | ✅ |
| `doch` جواباً للسؤال المنفيّ | `a1-08:t2` + النصّ | ✅ |
| `unser` · `euer` · `Ihr` | `a1-10:t4` | ✅ |
| ضمائر النصب mich/dich/ihn | `a1-11:t4` | ✅ |
| Wechselpräpositionen — الشقّ المنصوب | `a1-11:t2` | ✅ |

وأُضيفت في الطريق كتلةٌ خامسة لم تكن في الخريطة: حجز الموعد وإلغاؤه (`a1-09:t4`) — مادّة Sprechen Teil 3 في Goethe A1.

**باقٍ خام (39):**

| المستوى | دروس | متوسّط الشرح | نصوص قراءة |
|---|---|---|---|
| A1 | 3 (`a1-12` · `a1-13` · `a1-14`) | ~280 حرفاً | 0/3 |
| A2 | 13 | 274 | 0/13 |
| B1 | 12 | 267 | 0/12 |
| B2 | 11 | 280 | 0/11 |

**تنبيه قائم:** `a1-14` يعلّم اليوم الأرقام 0–100 — تكرارٌ لمادّة `a1-07`. القرار المثبَّت: يُعاد بناؤه درسَ **Perfekt** مع `wollen/sollen`.

---

## ٥. المفردات — الفجوة الكمّية

| المستوى | محفوظ اليوم (بطاقات) | مطلوب (Goethe) | العجز |
|---|---|---|---|
| A1 | 183 | ~650 | **−467** |
| A2 | 112 | ~1300 تراكمي | −٪ كبير |
| B1 | 103 | ~2400 تراكمي | −٪ كبير |

المفردات تمرّ فعلاً أمام المتعلّم (986 صورة كلمة ألمانية في نصوص A1)، لكنّها **لا تُرشَّح إلى بطاقات**. العلاج ليس تأليف مفردات جديدة بل **حصاد نصوص القراءة القائمة** — وهي مهمّة قابلة للأتمتة جزئياً، تُنفَّذ دفعةً واحدة بعد اكتمال دروس كل مستوى.

---

## ٦. ترتيب التنفيذ

1. **A1 حتى الاكتمال** — ~~`a1-08`~~ ~~`a1-09`~~ ~~`a1-10`~~ ~~`a1-11`~~ → `a1-12` · `a1-13` · `a1-14` (ثلاثة باقية)
2. **حصاد مفردات A1** — رفع 183 → ~650 بطاقة من النصوص القائمة
3. **إعادة بناء `a2-01`** على Perfekt المتقدّم (الأثر الارتداديّ)
4. **A2** — 12 درساً باقياً
5. **B1** — 12 درساً
6. **B2** — 11 درساً
7. **تصحيح `COVERAGE.md`** وتحديث ادّعاءات التغطية

بعد كل درس: `npm run typecheck` + `npm run lint` + كامل مجموعة الاختبارات + معاينة بصرية في متصفّح حقيقي.

---

## ٧. ما لا يُطلب من المستخدم

القرارات المعمارية (المرجع · موضع كل بنية نحوية · ترتيب التنفيذ · معالجة الآثار الارتدادية) **مسؤولية التنفيذ لا المستخدم**. تُوثَّق هنا وتُنفَّذ. ما يُعرض على المستخدم هو **النتيجة**: درسٌ مكتمل قابل للدراسة.
