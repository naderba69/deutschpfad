# 🔬 الجرد والتدقيق الشامل — DeutschPfad v5.7

> **التاريخ:** 2026-08-15 · **النتيجة:** 147 E2E + 149 وحدة · TSC 0 · ESLint 0
> **النطاق:** فحص برمجي + لغوي + منهجي لكل مكوّنات المشروع (59 مساراً، 112 ملف بيانات، 25 ملف اختبار)

---

## ① أخطاء برمجية اكتُشفت وأُصلحت (3 خطيرة)

### 1.1 💥 `/review/monthly` كان ينهار بصفحة خطأ (أُصلح)
- **الخطأ:** `TypeError: Cannot read properties of undefined (reading 'length')`
- **السبب:** `computeMonthSummary()` تعيد مفاتيح مختلفة عن `computeWeekSummary()` (`exercises/minutes/monthXp` بدل `exercisesAnswered/minutesStudied/weekXp`، وبلا `daysActive`). العميل يعاملهما كنوع واحد → انهيار عند قراءة `summary.daysActive.length`.
- **الإصلاح:** وحّدت `computeMonthSummary` لتعيد بنية `WeekSummary` كاملة.

### 1.2 💥 أخطاء hydration في 5 صفحات (أُصلحت)
- `/dashboard/five-minutes` و`/dashboard/review`: `useState(() => sample(...))` — **عشوائية في أول رندر** تجعل الخادم والعميل يختلفان → React #418/#422.
- `/dashboard` (progress-report): `new Date().toLocaleDateString` يختلف حسب المنطقة الزمنية.
- **كل الصفحات** (header): «…» على الخادم ثم «0%» بعد التحميل بلا `suppressHydrationWarning`.
- **الإصلاح:** نقل العشوائية إلى `useEffect`، وإضافة `suppressHydrationWarning` للتاريخ والتقدم.

### 1.3 💥 اكتشاف بيئي حاسم (خارج الكود)
- **45 اختباراً فشلت فجأة** رغم الكود السليم — السبب: **خادم قديم** (`next-server` pid=2135) ما زال حياً على المنفذ 3000 يقدم بناءً قديماً (chunks بـ 400 Bad Request).
- **الدرس:** أي `npm run build` جديد يجب أن يتبعه قتل كل عمليات `next-server` القديمة (`kill -9 <pid>`) قبل `npm run start`.

---

## ② فحص اللغة (ألمانية + عربية) — نظيف

فحصت 112 ملف بيانات بأنماط أخطاء شائعة:
| النمط | النتيجة |
|---|---|
| أخطاء إملائية عربية (هاذا/حتي/لأن…) | ✅ لا توجد |
| Umlaut ناقص (schon بدل schön) | ✅ جميع حالات «schon» صحيحة بمعنى «بالفعل» |
| تصريفات أفعال خاطئة | ✅ «ich bin gefahren» صحيحة (Perfekt مع sein) |
| أحرف غريبة/تالفة | ✅ لا توجد |

## ③ فحص المنهجية — كامل

- **المنهاج CEFR: 69/69 موضوعاً مغطى** (curriculum-audit: 69 ✓ كامل، 0 جزئي، 0 مفقود)
- **الدروس:** 46 درساً (A1:14, A2:12, B1:10, B2:10) — التسلسل سليم
- **المفردات:** 4576 كلمة فريدة موزعة على المستويات
- **المحتوى الإثرائي:** 60 قصة · 60 حواراً · 20 بودكاست · 16 بطاقة ثقافية

## ④ فحص الروابط — نظيف

- 59 مساراً فعلياً، 23 رابطاً داخلياً فُحصت كلها — لا روابط مكسورة (المسارات الديناميكية `/tests/level/[level]`، `/tests/seal/[level]`، `/lesson/[id]` كلها موجودة).

## ⑤ فحص مفاتيح React — نظيف

- كل `Array.map` في المكونات له `key` — لا تحذيرات مفاتيح مفقودة.

---

# 💡 اقتراحات التطوير (مرتبة بالأولوية)

## أولوية عالية (تجربة المستخدم)
1. **وضع عدم الاتصال الكامل**: تجميع كل الموارد (دروس + صوتيات) في ذاكرة التخزين المؤقت ليعمل الموقع كاملاً بلا إنترنت بعد أول زيارة — جوهري لفئة المستخدمين المستهدفة.
2. **مزامنة تقدم عبر الأجهزة** (اختياري بحساب): تصدير/استيراد سحابي عبر رابط تشفير — «أكمل على هاتفك وتابع على حاسوبك».
3. **دوري أصدقاء**: جدول متصدرين أسبوعي بين الأصدقاء عبر كود مشاركة (بدون خادم — عبر LocalStorage مشترك في رابط).
4. **مؤشر إتقان لكل مفردة** (SM-2 مرئي): نقاط إتقان لكل كلمة في القاموس — بدل الاكتفاء بالبطاقات.

## أولوية متوسطة (المحتوى)
5. **معجم مصوّر تفاعلي**: الصور الدلالية للألف كلمة الأكثر تكراراً (A1-A2) — ربط بصري يقوي الحفظ.
6. **جلسات محادثة حوارية محاكية (Chatbot)**: سيناريوهات «نادل/طبيب/موظف بنك» تتفاعل مع نطق المستخدم بلا LLM (محرك قرار محلي).
7. **تمارين تحويل الجمل بين الأزمنة** (Umformung) على نطاق أوسع — مهارة جوهرية في Goethe B1/B2.
8. **ملفات صوت حقيقية مسجلة** لأشهر 500 جملة (بدل TTS فقط) — جودة نطق بشرية.

## أولوية متوسطة (التقنية)
9. **CI/CD**: GitHub Actions (عند توفر Token بصلاحية workflow) يبني ويشغّل 147 E2E تلقائياً قبل كل نشر.
10. **تحليل الحزم**: تقليل First Load JS (حالياً 90KB مشترك) عبر dynamic import إضافي للمكونات الثقيلة.
11. **فحص أداء Lighthouse تلقائي** في الاختبارات (LCP < 2.5s، CLS < 0.1).

## أولوية مستقبلية (تتطلب خادماً)
12. **حسابات مستخدمين حقيقية** مع مزامنة سحابية — نقلة من «تقدم لكل متصفح» إلى «تقدم لكل مستخدم».
13. **سوق الجواهر**: صرف الجواهر في ثيمات وشارات ووضع ليلي مخصص.
14. **إصدار تطبيق Android** (TWA عبر PWA الحالية) — أيقونة أصلية بلا متجر.

---

# 📌 خلاصة الجولة

| البند | النتيجة |
|---|---|
| بوكسات النطق | ✅ 3 بلوكات عمودية (ألمانية/عربية/أزرار) كما طلبتِ |
| أخطاء برمجية | ✅ أُصلح 3 (انهيار monthly + hydration ×5 صفحات + خادم قديم) |
| فحص اللغة | ✅ نظيف |
| فحص المنهجية | ✅ 69/69 CEFR |
| فحص الروابط والمفاتيح | ✅ نظيف |
| الاختبارات | ✅ 147 E2E + 149 وحدة · TSC 0 · ESLint 0 |
